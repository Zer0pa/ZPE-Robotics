# zpe-motion-kernel 0.1.1

This legacy package exists only to redirect users to the renamed package:

```bash
pip install zpe-robotics
```

Importing `zpe_motion_kernel` emits a `DeprecationWarning` with that upgrade
instruction.
