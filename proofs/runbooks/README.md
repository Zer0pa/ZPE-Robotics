# Runbooks

This directory contains the current repo-relative Wave-1 runbooks.

Notes:

- proof output paths now point at `proofs/reruns/`
- the master runbook header was normalized for repo-relative use
- historical planning docs that still live in the outer workspace were not
  pulled into the repo as front-door material
