name: E-G3 Comparator

on:
  workflow_dispatch:

jobs:
  e_g3_comparator:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Set up uv
        uses: astral-sh/setup-uv@v7

      - name: Sync development environment
        run: uv sync --frozen --extra dev

      - name: Run E-G3 comparator
        run: |
          uv run python scripts/e_g3_comparator.py \
            --workflow-run-id "${GITHUB_RUN_ID}" \
            --output "proofs/reruns/e_g3_comparator_current/e_g3_comparator_result.json"

      - name: Upload E-G3 comparator evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: e_g3-comparator-evidence
          path: proofs/reruns/e_g3_comparator_current/e_g3_comparator_result.json
          if-no-files-found: error

      - name: Enforce decisive E-G3 result
        if: always()
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          path = Path("proofs/reruns/e_g3_comparator_current/e_g3_comparator_result.json")
          if not path.exists():
              raise SystemExit("E-G3 comparator artifact missing")
          payload = json.loads(path.read_text(encoding="utf-8"))
          if payload.get("status") != "PASS":
              raise SystemExit(f"E-G3 comparator failed: status={payload.get('status')}")
          PY
